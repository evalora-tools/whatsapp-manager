{
  "name": "whatsapp-manager",
  "version": "1.0.0",
  "description": "WhatsApp conversation management application with React and Node.js",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["whatsapp", "react", "nodejs", "supabase", "tailwind"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}